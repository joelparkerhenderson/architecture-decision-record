# 如何使用 git 開始使用 ADR

如果你喜歡使用 git 版本控制，以下是我們在一般含有原始碼的軟體專案中，開始使用 ADR 搭配 git 的方式。

建立一個用於存放 ADR 檔案的目錄：

```sh
$ mkdir adr
```

為每個 ADR 建立一個文字檔，例如 `database.txt`：

```sh
$ vi database.txt
```

在 ADR 中寫下你想記錄的任何內容。你可以參考本儲存庫中的範本來獲取靈感。

將 ADR 提交到你的 git 儲存庫。
